<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shows">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shows">
        <activity android:name=".views.SearchingActivity"></activity>
        <activity android:name=".views.BookingPageActivity" />
        <activity android:name=".views.FullyPerformActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".views.login.SignUpActivity" /> <!-- <activity android:name=".views.login.LoginActivity" /> -->
        <!-- <activity -->
        <!-- android:name=".views.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_autorization" /> -->
        <activity android:name=".views.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>